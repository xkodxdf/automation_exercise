package pages;

import org.openqa.selenium.WebDriver;
import pages.base_abstract.TopMenuPage;

public class CartPage extends TopMenuPage {

    public CartPage(WebDriver driver) {
        super(driver);
    }
}
